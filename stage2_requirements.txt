# Stage-2 OmniVoice/MLX runtime, local app, and test dependencies
# Core voice runtime pins are validated by the Apple-Silicon acceptance workflow.
# 0.4.6 is the latest mlx-audio release currently published on PyPI.
mlx-audio==0.4.6
mlx==0.32.0
mlx-lm==0.31.3
transformers==5.15.0
soundfile==0.14.0
numpy==2.4.6
pytest==9.1.1

# Local-only application layer pins.
fastapi==0.141.1
uvicorn==0.51.0
python-multipart==0.0.32
pypdf==6.15.0
